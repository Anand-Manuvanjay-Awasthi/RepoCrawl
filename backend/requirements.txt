fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
pydantic-settings>=2.4.0
python-dotenv>=1.0.1

# Vector store (Phase 3+)
chromadb>=0.5.0

# Embeddings + LLM via Hugging Face Inference API (Phase 3 / Phase 6) —
# no local torch/sentence-transformers runtime needed, keeping the backend
# lightweight; models run remotely on Hugging Face's infrastructure.
huggingface-hub>=0.24.0

# LangGraph orchestration for the RAG generation flow (Phase 6)
langgraph>=0.2.0

# Repository ingestion (used starting Phase 2)
GitPython>=3.1.43
requests>=2.32.0
